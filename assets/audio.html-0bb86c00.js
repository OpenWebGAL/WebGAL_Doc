import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as d,c as a,d as i}from"./app-46aaefde.js";const s={},n=i(`<h1 id="音频" tabindex="-1"><a class="header-anchor" href="#音频" aria-hidden="true">#</a> 音频</h1><h2 id="播放背景音乐-bgm" tabindex="-1"><a class="header-anchor" href="#播放背景音乐-bgm" aria-hidden="true">#</a> 播放背景音乐（BGM）</h2><p>播放背景音乐的方法极其简单，你只需要将背景音乐放置在 <code>bgm</code> 文件夹下，然后使用 <code>bgm</code> 指令来播放背景音乐。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:夏影.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>可以为背景音乐设置一个 <code>-volume</code> 参数，来调整它的音量。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:夏影.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>此外，还可以使用 <code>-enter</code> 参数，进行淡入播放。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:夏影.mp3 -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>最后，可以使用以下语句，进行背景音乐的淡出。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:none -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">提示</p><p><code>-volume</code> 和 <code>-enter</code> 都是可选参数。<br><code>-volume</code> 未使用时，或使用了 0 ~ 100 以外的值时，将使用默认值 100。<br><code>-enter</code> 未使用时，或使用了 0 ~ 以外的值时，将使用默认值 0。</p></div><h2 id="播放语音" tabindex="-1"><a class="header-anchor" href="#播放语音" aria-hidden="true">#</a> 播放语音</h2><p>众所周知，视觉小说 最吸引人的地方往往在于其有优秀的配音。将配音文件放入 <code>vocal</code> 文件夹，然后在对话脚本中加入 <code>-语音文件名</code> 参数，就可以播放语音了。语法示例如下。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>比企谷八幡:刚到而已 -V3.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在连续对话时，语音的引入方式也是一样的。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>雪之下雪乃:你到得真早 -V1.ogg;
对不起，等很久了吗？ -V2.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>同时可以设置一个 <code>-volume</code> 参数，来调整它的音量。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>比企谷八幡:刚到而已 -V3.ogg -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">提示</p><p><code>-volume</code> 是可选参数。<br><code>-volume</code> 未设置时，或使用了 0 ~ 100 以外的值时，将使用默认值 100。</p></div><h2 id="播放效果音" tabindex="-1"><a class="header-anchor" href="#播放效果音" aria-hidden="true">#</a> 播放效果音</h2><p>将你的配音放入 <code>vocal</code> 文件夹之后，再使用 <code>playEffect</code> 指令就可以播放效果音了<sup>_</sup></p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>可以为效果音设置一个 <code>-volume</code> 参数，来调整它的音量。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">提示</p><p><code>-volume</code> 是可选参数。<br><code>-volume</code> 未设置时，或使用了 0 ~ 100 以外的值时，将使用默认值 100。</p></div><h3 id="效果音循环" tabindex="-1"><a class="header-anchor" href="#效果音循环" aria-hidden="true">#</a> 效果音循环</h3><p>为效果音赋予一个 <code>id</code> 将会自动启用效果音循环，后续使用相同的 <code>id</code> 来停止。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -id=xxx;
playEffect:none -id=xxx; // 停止这个循环的效果音
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="解锁音频以供鉴赏" tabindex="-1"><a class="header-anchor" href="#解锁音频以供鉴赏" aria-hidden="true">#</a> 解锁音频以供鉴赏</h2><p>使用 <code>unlockBgm</code> 解锁音频鉴赏中的音频。</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>; // 解锁bgm并赋予名称
unlockBgm:s_Title.mp3 -name=Smiling-Swinging!!!;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,31),c=[n];function l(r,o){return d(),a("div",null,c)}const v=e(s,[["render",l],["__file","audio.html.vue"]]);export{v as default};
