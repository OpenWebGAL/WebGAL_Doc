import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as n,c as a,d as i}from"./app-d9fcea27.js";const t={},s=i(`<h1 id="scene-and-branch" tabindex="-1"><a class="header-anchor" href="#scene-and-branch" aria-hidden="true">#</a> Scene and Branch</h1><p>In Visual Novels, jumping to chapters, scenes and selecting branches are indispensable, so WebGAL also supports scene jumping and branch selection.</p><p>You can split your script into multiple txt documents, and use a simple statement to switch the currently running script.</p><div class="hint-container warning"><p class="hint-container-title">Note</p><p>After jumping scenes, selecting branches or calling scenes, the stage will not be cleared. This means that the background music, portraits, background images and other effects applied in the previous scene will be inherited to the next scene.</p><p>If you are using WebGAL Terre visual editor, you should pay special attention to it, because the editor does not care about the effects of the previous scene when previewing a single scene. Therefore, the preview effect and the actual game running effect may be different. You should consider handling the stage cleanup work before the scene jump properly.</p></div><h2 id="scene-jumping" tabindex="-1"><a class="header-anchor" href="#scene-jumping" aria-hidden="true">#</a> Scene Jumping</h2><p>Suppose you have written two chapters of the script, namely <code>Chapter-1.txt</code> and <code>Chapter-2.txt</code>. After <code>Chapter-1.txt</code> is finished running, you want to jump to the scene corresponding to <code>Chapter-2.txt</code>, please use the following statement:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>changeScene:Chapter-2.txt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>(Chapter-1.txt)
......
......
; // The following is the content of Chapter-2.txt
changeScene:Chapter-2.txt;
......
......
(Chapter-2.txt)
......
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>By executing this command, you will switch the game scene and make the subsequent plot development run according to the new scene script. The new script will run after the next mouse click.</p><h2 id="scene-calling" tabindex="-1"><a class="header-anchor" href="#scene-calling" aria-hidden="true">#</a> Scene Calling</h2><p>If you need to call another scene in a scene, you can use <code>callScene</code>, and the original scene will be returned after the called scene is finished running.</p><p>Statement:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>callScene:Chapter-2.txt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Example:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>(Chapter-1.txt)
......
......
callScene:Chapter-2.txt;
; // The following is the content of Chapter-2.txt
......
......
(Chapter-2.txt)
......
......
(Chapter-2.txt execution completed)
; // Return to the parent scene and continue to execute the parent scene statement
......
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="branch-selection" tabindex="-1"><a class="header-anchor" href="#branch-selection" aria-hidden="true">#</a> Branch Selection</h2><p>If there are branch options in your script, and you want to enter different chapters by selecting different options, please use <code>choose</code>.<br> Use <code>Option text: Chapter file name</code> to define an option. Use <code>|</code> to separate different options. An example is as follows:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>choose:Stop her:Chapter-2.txt|Go home:Chapter-3.txt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You only need to match the text of the option with the name of the script to be entered after the option is selected, and you can realize the function of branch selection.</p><h3 id="conditional-display-and-selection" tabindex="-1"><a class="header-anchor" href="#conditional-display-and-selection" aria-hidden="true">#</a> Conditional Display and Selection</h3><p>You can also conditionally display choices or allow the user to click on them based on variables, with the following syntax:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>choose:(showConditionVar&gt;1)[enableConditionVar&gt;2]-&gt;Call out to her:Chapter-2.txt|Go home:Chapter-3.txt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Here, the first choice will only be displayed if showConditionVar is greater than 1, and the user will only be allowed to click on it if enableConditionVar is greater than 2.</p><h2 id="label-jump" tabindex="-1"><a class="header-anchor" href="#label-jump" aria-hidden="true">#</a> Label Jump</h2><p>If you want to create a branch, but you think it is too troublesome to create a new TXT file for this, you can try the following method to create a branch and jump statement in the same file.</p><div class="hint-container warning"><p class="hint-container-title">Note</p><p>If your branch is very long, I do not recommend that you use this method, because the number of lines in a TXT should not be too long, otherwise it may cause performance problems such as slow loading and slow response.</p></div><h3 id="create-a-label-label" tabindex="-1"><a class="header-anchor" href="#create-a-label-label" aria-hidden="true">#</a> Create a label (<code>label</code>)</h3><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>......
jumpLabel:label_1; // Jump to label_1
......
......
label:label_1; // Create a label named label_1
......
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In short, <code>jumpLabel</code> is similar to the <code>goto</code> statement, which can immediately jump your script to any position in the scene (TXT file), and this position needs to be created by you with <code>label</code>.</p><p>If <code>jumpLabel</code> is compared to any door, then the end point of this any door is the position of <code>label</code>.</p><h3 id="add-options" tabindex="-1"><a class="header-anchor" href="#add-options" aria-hidden="true">#</a> Add options</h3><p>With the above foundation, you can use <code>choose</code> to implement the use of branches to jump to the position of <code>label</code>:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>......
choose:Branch 1:label_1|Branch 2:label_2;
label:label_1; // Create a label named label_1
......
......
jumpLabel:end; // Jump to end
label:label_2; // Create a label named label_2
......
......
jumpLabel:end; // Jump to end
label:end; // Create a label named end
......
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Note that at the end of each branch, you should use <code>jumpLabel</code> to jump to the position you want. Since the program is executed linearly, if you do not jump to a location at the end of the branch, the program will continue to run down, for example:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>......
choose:Branch 1:label_1|Branch 2:label_2;
label:label_1; // Create a label named label_1
......
......
label:label_2; // Create a label named label_2
......
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this script, if you select <code>Branch 2</code>, everything seems to be fine. But if you select <code>Branch 1</code>, you will be surprised to find that after <code>Branch 1</code> is executed, <code>Branch 2</code> will continue to be executed. That&#39;s because the program continues to execute the next line in order, and you didn&#39;t specify where to jump after the branch ended.</p>`,37),l=[s];function d(o,c){return n(),a("div",null,l)}const u=e(t,[["render",d],["__file","scenes.html.vue"]]);export{u as default};
