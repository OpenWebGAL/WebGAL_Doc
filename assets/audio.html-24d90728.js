import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c as i,d}from"./app-1c6f6712.js";const n={},s=d(`<h1 id="audio" tabindex="-1"><a class="header-anchor" href="#audio" aria-hidden="true">#</a> Audio</h1><h2 id="playing-background-music-bgm" tabindex="-1"><a class="header-anchor" href="#playing-background-music-bgm" aria-hidden="true">#</a> Playing Background Music (BGM)</h2><p>Playing background music is extremely simple, you just need to put the background music in the <code>bgm</code> folder, and then use the <code>bgm</code> instruction to play the background music.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:Summer_Shadow.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can set a <code>-volume</code> parameter for the background music to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:Summer_Shadow.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In addition, you can also use the <code>-enter</code> parameter to fade in the playback.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:Summer_Shadow.mp3 -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Finally, you can use the following statement to fade out the background music.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:none -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> and <code>-enter</code> are both optional parameters.<br> When <code>-volume</code> is not used, or a value other than 0 to 100 is used, the default value of 100 will be used.<br> When <code>-enter</code> is not used, or a value other than 0 to is used, the default value of 0 will be used.</p></div><h2 id="playing-voice" tabindex="-1"><a class="header-anchor" href="#playing-voice" aria-hidden="true">#</a> Playing Voice</h2><p>As we all know, the most attractive part of visual novels is often their excellent voice acting. Put the voice acting file in the <code>vocal</code> folder, and then add the <code>-Voice file name</code> parameter in the dialog script to play the voice. The syntax is as follows.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Hachiman Hikigaya:Just arrived -V3.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>When talking continuously, the way the voice is introduced is the same.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Yukino Yukinoshita:You&#39;re here early -V1.ogg;
Sorry, have you been waiting long? -V2.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>At the same time, you can set a <code>-volume</code> parameter to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Hachiman Hikigaya:Just arrived -V3.ogg -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> is an optional parameter.<br> When <code>-volume</code> is not set, or a value other than 0 to 100 is used, the default value of 100 will be used.</p></div><h2 id="playing-sound-effects" tabindex="-1"><a class="header-anchor" href="#playing-sound-effects" aria-hidden="true">#</a> Playing Sound Effects</h2><p>After putting your dubbing into the <code>vocal</code> folder, you can use the <code>playEffect</code> instruction to play the sound effects<sup>_</sup></p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can set a <code>-volume</code> parameter for the sound effect to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> is an optional parameter.<br> When <code>-volume</code> is not set, or a value other than 0 to 100 is used, the default value of 100 will be used.</p></div><h3 id="sound-effect-loop" tabindex="-1"><a class="header-anchor" href="#sound-effect-loop" aria-hidden="true">#</a> Sound Effect Loop</h3><p>Giving the sound effect an <code>id</code> will automatically enable the sound effect loop, and the same <code>id</code> will be used to stop it later.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -id=xxx;
playEffect:none -id=xxx; // Stop this looping sound effect
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="unlock-audio-for-appreciation" tabindex="-1"><a class="header-anchor" href="#unlock-audio-for-appreciation" aria-hidden="true">#</a> Unlock Audio for Appreciation</h2><p>Use <code>unlockBgm</code> to unlock the audio in the audio appreciation.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>; // Unlock bgm and give it a name
unlockBgm:s_Title.mp3 -name=Smiling-Swinging!!!;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,31),o=[s];function t(l,c){return a(),i("div",null,o)}const m=e(n,[["render",t],["__file","audio.html.vue"]]);export{m as default};
