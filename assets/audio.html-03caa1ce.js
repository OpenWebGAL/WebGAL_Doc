import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c as i,d}from"./app-72f086ed.js";const n={},s=d(`<h1 id="audio" tabindex="-1"><a class="header-anchor" href="#audio" aria-hidden="true">#</a> Audio</h1><h2 id="play-background-music-bgm" tabindex="-1"><a class="header-anchor" href="#play-background-music-bgm" aria-hidden="true">#</a> Play background music (BGM)</h2><p>The way to play background music is extremely simple. You just need to place the background music in the <code>bgm</code> directory, then you can simply play it when needed:</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:XiaYing.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can set a <code>-volume</code> parameter for background music to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:XiaYing.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In addition, you can use the <code>-enter</code> parameter for fade-in playback.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:XiaYing.mp3 -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Finally, you can use the following statement to fade out the background music.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>bgm:none -enter=3000;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> and <code>-enter</code> are optional parameters.<br> When <code>-volume</code> is not used, or when a value other than 0 ~ 100 is used, the default value of 100 is used.<br> When <code>-enter</code> is not used, or when a value other than 0 ~ is used, the default value of 0 is used.</p></div><h2 id="play-voice" tabindex="-1"><a class="header-anchor" href="#play-voice" aria-hidden="true">#</a> Play voice</h2><p>As we all know, what often attracts people most to visual novels is the excellent dubbing. Put your dubbing in the <code>vocal</code> folder, then add <code>-audio_file_name</code> parameters to play it.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Yui: I just got here -V3.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The introduction of dubbing is the same in continuous dialogue.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Yukino: You&#39;re here early -V1.ogg;  
Sorry, have you been waiting long? -V2.ogg;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>You can set a <code>-volume</code> parameter for a voice to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>Yui: I just got here -V3.ogg -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> is an optional parameter.<br> When <code>-volume</code> is not used, or when a value other than 0 ~ 100 is used, the default value of 100 is used.</p></div><h2 id="play-sound-effects" tabindex="-1"><a class="header-anchor" href="#play-sound-effects" aria-hidden="true">#</a> Play sound effects</h2><p>You just need to place the sound effects in the <code>vocal</code> directory, then you can use <code>playEffect</code> to play it.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can set a <code>-volume</code> parameter for sound effects to adjust its volume.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -volume=30;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>-volume</code> is an optional parameter.<br> When <code>-volume</code> is not used, or when a value other than 0 ~ 100 is used, the default value of 100 is used.</p></div><h3 id="loop-sound-effects" tabindex="-1"><a class="header-anchor" href="#loop-sound-effects" aria-hidden="true">#</a> Loop sound effects</h3><p>Giving an id to a sound effect will automatically enable sound effect looping. Use the same id later to stop it.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>playEffect:xxx.mp3 -id=xxx;  
playEffect:none -id=xxx; // Stop this looping sound effect
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="unlock-audio-for-appreciation" tabindex="-1"><a class="header-anchor" href="#unlock-audio-for-appreciation" aria-hidden="true">#</a> Unlock audio for appreciation</h2><p>Use <code>unlockBgm</code> to unlock audio in Audio Appreciation.</p><div class="language-ws line-numbers-mode" data-ext="ws"><pre class="language-ws"><code>; // Unlock the bgm and give it a name.
unlockBgm:s_Title.mp3 -name=Smiling-Swinging!!!;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,31),o=[s];function l(t,c){return a(),i("div",null,o)}const p=e(n,[["render",l],["__file","audio.html.vue"]]);export{p as default};
